<?xml version="1.0" encoding="utf-8"?>
<ripple
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/colorPrimary">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <corners android:radius="10dp"/>
            <solid android:color="@color/colorPrimary"/>
        </shape>

    </item>




    <!-- The use of the id @android:id/mask declares this as being a mask,
    and the mask defines the bounds for the ripple animation.
    I have included a solid fill within the shape drawable which defines the mask area.  -->

</ripple>